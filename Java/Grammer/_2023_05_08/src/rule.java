public class rule {
    public static void main(String[] args) {

        /*
        class Cookie {}
        class ChocoCookie {}
        클래스명 규칙
        1. 클래스명은 명사로 한다
        2. 여러개의 단어가 섞이는 경우 각 단어의 첫번째 문자는 대문자 (Camel Case, Pascal Case)
        */

        /*
        run();
        runFast();
        getBackground();
        메서드명 규칙
        1. 메서드명은 동사로 한다
        2. 클래스명과 마찬가지로 여러개의 단어가 섞는 경우 각 단어의 첫번째 문자는 대문자, 단, 처음 시작하는 문자는 소문자
        */

        String userName;
        float lineWidth;
        int i;  // 반복문에서 사용
        char c;  // 반복문에서 사용
        /*
        변수명 규칙
        1. 변수명을 통해 변수의 사용 의도를 알 수 있게 지어야함
        2. 순서를 의미하는 임시적인 정수의 변수명은 i, j, k, m, n을 사용
        3. 변수명에 _, $ 기호를 사용할 수 있지만 시작 문자로 사용하지 않는다
        */

    }
}
